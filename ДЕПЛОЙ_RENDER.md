# 🚀 ДЕПЛОЙ НА RENDER.COM

Пошаговая инструкция по деплою бота Рекламный Инспектор на Render.com

---

## ✅ Преимущества Render

- ✅ Не требует GitHub для регистрации (можно через Email)
- ✅ Автоматический деплой из GitHub (если подключишь)
- ✅ Автоперезапуск при падении бота
- ✅ Логи в реальном времени
- ✅ Встроенный PostgreSQL
- ✅ SSL из коробки
- ✅ Простой интерфейс

---

## 📋 Шаг 1: Регистрация на Render

### 1.1. Открой сайт Render

1. Перейди на [render.com](https://render.com)
2. Нажми **"Get Started for Free"** или **"Sign Up"**

### 1.2. Выбери способ регистрации

**Вариант 1: Email (рекомендуется)**
- Нажми **"Sign up with Email"**
- Введи email и пароль
- Подтверди email (проверь почту)

**Вариант 2: GitHub**
- Если GitHub работает — можешь использовать
- Нажми **"Sign up with GitHub"**

### 1.3. Подтверждение

- Проверь почту
- Найди письмо от Render
- Нажми ссылку подтверждения
- Готово!

---

## 📋 Шаг 2: Подготовка кода

### 2.1. Создай репозиторий на GitHub (рекомендуется)

Если еще нет репозитория:

```bash
cd "/Users/ya_neona/Library/Mobile Documents/com~apple~CloudDocs/Cursor/Бизнес/Команда Яны Рыжей/99_Системное/рекламный_инспектор_bot"

# Инициализируй Git (если еще не сделано)
git init

# Добавь файлы
git add .

# Создай коммит
git commit -m "Готово к деплою на Render"

# Создай репозиторий на GitHub и подключи
git remote add origin https://github.com/твой_username/рекламный_инспектор_bot.git
git branch -M main
git push -u origin main
```

**Важно:** Не коммить `.env` файл! Он уже в `.gitignore`

### 2.2. Альтернатива: Загрузка кода вручную

Если не хочешь использовать GitHub:
- Можно загрузить код через веб-интерфейс Render
- Но автоматический деплой работать не будет

---

## 📋 Шаг 3: Создание PostgreSQL базы данных

1. В Render Dashboard нажми **"+ New"**
2. Выбери **"PostgreSQL"**
3. Настройки:
   - **Name:** `reklamnyi-inspector-db` (или любое имя)
   - **Database:** `reklamnyi_inspector` (или любое имя)
   - **User:** `reklamnyi_inspector` (или любое имя)
   - **Region:** Выбери ближайший (например, Frankfurt)
   - **Plan:** **Free** (для теста) или **Starter** ($7/месяц)
4. Нажми **"Create Database"**

**Важно:** Запомни или скопируй **Internal Database URL** — он понадобится!

---

## 📋 Шаг 4: Создание Background Worker (бот)

**Важно:** Для Telegram-бота нужен **Background Worker**, а не Web Service!

**Почему Background Worker:**
- Telegram-бот использует long polling (не webhooks)
- Бот работает постоянно, а не отвечает на HTTP запросы
- Background Worker идеально подходит для таких задач

1. В Render Dashboard нажми **"+ New"**
2. Выбери **"Background Worker"** (не Web Service!)
3. Подключи репозиторий:
   - Если есть GitHub: **"Connect account"** → выбери репозиторий
   - Если нет GitHub: **"Public Git repository"** → вставь URL репозитория
   - Или **"Deploy from a public Git repository"**

### 4.1. Настройки сервиса:

**Basic:**
- **Name:** `reklamnyi-inspector-bot` (или любое имя)
- **Region:** Выбери тот же что и для PostgreSQL
- **Branch:** `main` (или `master`)
- **Root Directory:** Оставь пустым (если код в корне)

**Build & Deploy:**
- **Build Command:** Оставь пустым (Render определит автоматически)
- **Start Command:** `python bot.py`

**Environment:**
- **Environment:** `Python 3`
- **Python Version:** `3.11` (или последняя доступная)

### 4.2. Переменные окружения:

Нажми **"Add Environment Variable"** и добавь:

| Переменная | Значение | Где взять |
|------------|----------|-----------|
| `TELEGRAM_BOT_TOKEN` | Твой токен бота | @BotFather в Telegram |
| `ADMIN_CHAT_ID` | Твой chat_id | @userinfobot в Telegram |
| `DATABASE_URL` | Внутренний URL базы | Из настроек PostgreSQL (Internal Database URL) |
| `LOG_LEVEL` | `INFO` | По умолчанию |

**Как получить токен:**
- Открой [@BotFather](https://t.me/BotFather)
- Отправь `/mybots` → выбери бота → **API Token**

**Как получить chat_id:**
- Открой [@userinfobot](https://t.me/userinfobot)
- Он покажет твой chat_id

**Как получить DATABASE_URL:**
- В Render Dashboard открой PostgreSQL сервис
- Во вкладке **"Info"** найди **"Internal Database URL"**
- Скопируй его и вставь в переменную `DATABASE_URL`

### 4.3. План:

- **Free** — для теста (может "засыпать" после 15 минут бездействия)
- **Starter** ($7/месяц) — для продакшена (работает 24/7)

### 4.4. Создание:

Нажми **"Create Background Worker"**

---

## 📋 Шаг 5: Ожидание деплоя

Render автоматически:
1. Склонирует код из GitHub
2. Установит зависимости из `requirements.txt`
3. Запустит бота

**Время:** 2-5 минут

**Проверь:**
- Вкладка **"Logs"** — должны быть логи установки и запуска
- Ожидаемые логи:
  ```
  INFO - 🔍 Рекламный Инспектор запущен!
  ```

---

## 📋 Шаг 6: Проверка работы

1. Открой бота в Telegram
2. Отправь `/start`
3. Пройди регистрацию
4. Отправь URL для проверки
5. Получи PDF-отчет

**Если бот не отвечает:**
- Проверь логи в Render (вкладка "Logs")
- Проверь переменные окружения
- Убедись что токен правильный
- Убедись что `DATABASE_URL` правильный

---

## 🔄 Обновление бота

### Автоматически (если подключен GitHub):

1. Внеси изменения в код локально
2. Закоммить и запушить в GitHub:
   ```bash
   git add .
   git commit -m "Обновление бота"
   git push
   ```
3. Render автоматически задеплоит новую версию

### Вручную:

1. В Render открой Web Service
2. Нажми **"Manual Deploy"** → **"Deploy latest commit"**

---

## 📊 Мониторинг

### Логи в реальном времени:
- Render → Web Service → **"Logs"**
- Видно все логи бота в реальном времени

### Метрики:
- Render → Web Service → **"Metrics"**
- CPU, память, количество запросов

### Статус:
- Render → Web Service → **"Events"**
- История деплоев и их статусы

---

## 🐛 Troubleshooting

### Бот не запускается

**Проверь:**
1. Логи в Render (вкладка "Logs")
2. Переменные окружения (все ли заполнены)
3. Правильность токена бота
4. Правильность `DATABASE_URL`

**Частые ошибки:**

**"TELEGRAM_BOT_TOKEN не установлен"**
- Добавь переменную `TELEGRAM_BOT_TOKEN` в Render

**"Ошибка подключения к базе данных"**
- Проверь что PostgreSQL сервис создан
- Проверь что `DATABASE_URL` правильный (Internal Database URL)
- Убедись что оба сервиса в одном регионе

**"Module not found"**
- Проверь `requirements.txt` — все зависимости должны быть указаны
- Проверь логи — там будет видно какие модули не найдены

**"Free plan засыпает"**
- На Free плане бот "засыпает" после 15 минут бездействия
- Решение: перейти на Starter план ($7/месяц)

---

## 💰 Стоимость

**Render Free Plan:**
- Бесплатно
- Ограничения:
  - Бот "засыпает" после 15 минут бездействия
  - Медленнее запуск после "сна"
  - Подходит только для теста

**Render Starter Plan:**
- $7/месяц (~700₽)
- Включает:
  - Работает 24/7 без "сна"
  - Быстрый запуск
  - Больше ресурсов
  - Подходит для продакшена

**PostgreSQL:**
- Free план — бесплатно (с ограничениями)
- Starter план — $7/месяц (для продакшена)

**Итого для продакшена:** $14/месяц (~1400₽)

---

## 🔒 Безопасность

**Важно:**
- ✅ `.env` файл в `.gitignore` — не попадет в GitHub
- ✅ Переменные окружения хранятся в Render (зашифрованы)
- ✅ PostgreSQL доступна только внутри Render (Internal URL)
- ✅ SSL включен автоматически

---

## 📞 Поддержка

**Проблемы с Render:**
- Документация: [render.com/docs](https://render.com/docs)
- Discord: [discord.gg/render](https://discord.gg/render)
- Email: support@render.com

**Проблемы с ботом:**
- Проверь логи в Render
- Проверь переменные окружения
- Убедись что код задеплоен правильно

---

## ✅ Чеклист деплоя

После деплоя проверь:

- [ ] Код залит в GitHub (или загружен вручную)
- [ ] PostgreSQL создан
- [ ] Web Service создан
- [ ] Переменные окружения настроены:
  - [ ] `TELEGRAM_BOT_TOKEN`
  - [ ] `ADMIN_CHAT_ID`
  - [ ] `DATABASE_URL` (Internal Database URL)
- [ ] Деплой завершен успешно
- [ ] Логи показывают запуск бота
- [ ] Бот отвечает в Telegram на `/start`
- [ ] Регистрация работает
- [ ] Проверка материалов работает
- [ ] PDF генерируется и отправляется

---

## 🎯 Отличия от Railway

| Параметр | Railway | Render |
|----------|---------|--------|
| Регистрация | Требует GitHub | Email или GitHub |
| Цена | $5/месяц | $7/месяц (Starter) |
| Free план | Есть (с кредитами) | Есть (но "засыпает") |
| PostgreSQL | Автоматически | Создается отдельно |
| Интерфейс | Современный | Простой |

---

**Готово!** 🎉

Бот работает на Render и автоматически обновляется при каждом push в GitHub (если подключен GitHub).

**Следующий шаг:** Протестируй бота и начни собирать лиды!
