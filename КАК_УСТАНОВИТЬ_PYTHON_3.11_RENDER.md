# 🐍 КАК УСТАНОВИТЬ PYTHON 3.11 НА RENDER

Render использует Python 3.13 по умолчанию, но нам нужен Python 3.11 для совместимости с библиотеками.

---

## 📋 Способ 1: Через Build Command (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Открой Background Worker

1. Открой [render.com](https://render.com)
2. Найди Background Worker `reklamnyi-inspector-bot`
3. Нажми на его название

### Шаг 2: Открой Settings

1. Вверху страницы найди вкладки: **"Logs"**, **"Metrics"**, **"Environment"**, **"Settings"**
2. Нажми на вкладку **"Settings"**

### Шаг 3: Измени Build Command

1. Прокрути вниз до раздела **"Build & Deploy"**
2. Найди поле **"Build Command"**
3. Вставь туда:
   ```bash
   pip install -r requirements.txt
   ```
4. Или оставь пустым (Render сам определит)

### Шаг 4: Проверь Environment

1. В разделе **"Environment"** убедись что есть переменная:
   - **Key:** `PYTHON_VERSION`
   - **Value:** `3.11.9`
   
   Если нет — добавь:
   - Нажми **"Add Environment Variable"**
   - Key: `PYTHON_VERSION`
   - Value: `3.11.9`
   - Save

### Шаг 5: Перезапусти деплой

1. Нажми кнопку **"Manual Deploy"** или **"Redeploy"**
2. Выбери **"Deploy latest commit"**
3. Подожди 2-3 минуты

---

## 📋 Способ 2: Через runtime.txt (уже создан)

Файл `runtime.txt` уже создан в репозитории с содержимым:
```
python-3.11.9
```

Но Render может его не читать. Попробуй:

1. Убедись что файл `runtime.txt` в корне репозитория (он там)
2. Перезапусти деплой вручную:
   - Settings → Manual Deploy → Deploy latest commit

---

## 📋 Способ 3: Через Build Command

Если предыдущие способы не работают:

1. Settings → Build & Deploy
2. Найди поле **"Build Command"**
3. Вставь:
   ```bash
   python3.11 -m pip install -r requirements.txt
   ```
4. Или оставь пустым (Render сам определит)

---

## ✅ Проверка

После изменения версии Python:

1. Открой вкладку **"Logs"**
2. В начале логов должно быть:
   ```
   Using Python version 3.11.9
   ```
   или
   ```
   Python 3.11.9
   ```

Если видишь `Python 3.13` — версия не изменилась, попробуй другой способ.

---

## 🐛 Если ничего не помогает

**Альтернатива:** Используй Python 3.12 вместо 3.11:

1. Settings → Python Version → `3.12`
2. Или в `runtime.txt` измени на: `python-3.12.7`

Python 3.12 тоже должен работать с нашими библиотеками.

---

**Главное:** Нужно заставить Render использовать Python 3.11 или 3.12 вместо 3.13! 🐍
