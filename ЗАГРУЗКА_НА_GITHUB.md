# 📤 ЗАГРУЗКА БОТА НА GITHUB

Пошаговая инструкция по загрузке кода бота в GitHub репозиторий

---

## 🎯 Зачем это нужно

После загрузки на GitHub можно:
- ✅ Подключить репозиторий к Render (автоматический деплой)
- ✅ Хранить код в облаке (резервная копия)
- ✅ Отслеживать изменения (версии)
- ✅ Работать с кодом с разных компьютеров

---

## 📋 Шаг 1: Создать репозиторий на GitHub

### 1.1. Открой GitHub

1. Перейди на [github.com](https://github.com)
2. Войди в аккаунт (или создай новый)

### 1.2. Создай новый репозиторий

1. Нажми **"+"** в правом верхнем углу
2. Выбери **"New repository"**

### 1.3. Настройки репозитория

**Repository name:**
- `reklamnyi-inspector-bot` (или любое имя)

**Description (опционально):**
- `Telegram bot для проверки рекламы банкротства на соответствие ФЗ`

**Visibility:**
- **Public** — все видят код (можно выбрать)
- **Private** — только ты видишь код (рекомендуется)

**Initialize:**
- ❌ НЕ ставь галочки (README, .gitignore, license)
- Мы уже все это создали локально

### 1.4. Создать репозиторий

Нажми **"Create repository"**

**Важно:** Скопируй URL репозитория! Он будет такой:
```
https://github.com/твой_username/reklamnyi-inspector-bot.git
```

---

## 📋 Шаг 2: Подготовка локального кода

### 2.1. Открой терминал

На Mac:
- Открой Terminal (Finder → Applications → Utilities → Terminal)
- Или нажми `Cmd + Space` → введи "Terminal"

### 2.2. Перейди в папку с ботом

```bash
cd "/Users/ya_neona/Library/Mobile Documents/com~apple~CloudDocs/Cursor/Бизнес/Команда Яны Рыжей/99_Системное/рекламный_инспектор_bot"
```

**Проверь что ты в правильной папке:**
```bash
ls
```

Должны увидеть файлы: `bot.py`, `requirements.txt`, `README.md` и т.д.

---

## 📋 Шаг 3: Инициализация Git

### 3.1. Проверь, есть ли уже Git

```bash
git status
```

**Если видишь ошибку "not a git repository":**
- Git еще не инициализирован, продолжай

**Если видишь список файлов:**
- Git уже инициализирован, переходи к шагу 4

### 3.2. Инициализируй Git

```bash
git init
```

Должно быть: `Initialized empty Git repository...`

---

## 📋 Шаг 4: Проверь .gitignore

### 4.1. Убедись что .gitignore есть

```bash
cat .gitignore
```

Должен быть файл с содержимым (`.env`, `data/`, `__pycache__/` и т.д.)

**Если файла нет:**
- Он уже создан, все ок

### 4.2. Проверь что .env не попадет в Git

```bash
ls -la | grep .env
```

Если видишь `.env` — это нормально, он не попадет в Git благодаря `.gitignore`

---

## 📋 Шаг 5: Добавь файлы в Git

### 5.1. Добавь все файлы

```bash
git add .
```

### 5.2. Проверь что добавилось

```bash
git status
```

Должны увидеть список файлов (но НЕ должно быть `.env`!)

**Если видишь `.env` в списке:**
- Проверь `.gitignore` — там должна быть строка `.env`

---

## 📋 Шаг 6: Создай первый коммит

```bash
git commit -m "Initial commit: Рекламный Инспектор бот"
```

Должно быть: `[main (root-commit) ...] Initial commit...`

---

## 📋 Шаг 7: Подключи GitHub репозиторий

### 7.1. Добавь remote

Замени `твой_username` на свой GitHub username:

```bash
git remote add origin https://github.com/твой_username/reklamnyi-inspector-bot.git
```

**Если у тебя уже есть remote:**
```bash
git remote set-url origin https://github.com/твой_username/reklamnyi-inspector-bot.git
```

### 7.2. Проверь remote

```bash
git remote -v
```

Должно показать URL твоего репозитория

---

## 📋 Шаг 8: Загрузи код на GitHub

### 8.1. Переименуй ветку (если нужно)

```bash
git branch -M main
```

### 8.2. Загрузи код

```bash
git push -u origin main
```

**Если GitHub попросит авторизацию:**
- Введи логин и пароль GitHub
- Или используй Personal Access Token (см. ниже)

---

## 🔐 Авторизация на GitHub

### Если просит пароль:

GitHub больше не принимает пароли через HTTPS. Нужен Personal Access Token:

### 1. Создай токен

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажми **"Generate new token"**
3. Название: `Render Deploy`
4. Выбери срок: `90 days` (или `No expiration`)
5. Отметь галочки:
   - ✅ `repo` (полный доступ к репозиториям)
6. Нажми **"Generate token"**
7. **Скопируй токен!** (показывается только один раз)

### 2. Используй токен вместо пароля

При `git push`:
- Username: твой GitHub username
- Password: вставь токен (не пароль!)

---

## ✅ Проверка

### 1. Открой репозиторий на GitHub

Перейди на:
```
https://github.com/твой_username/reklamnyi-inspector-bot
```

Должны увидеть все файлы бота!

### 2. Проверь что .env НЕ загружен

- В списке файлов НЕ должно быть `.env`
- Если есть — удали его из репозитория (см. ниже)

---

## 🐛 Troubleshooting

### Ошибка: "remote origin already exists"

**Решение:**
```bash
git remote remove origin
git remote add origin https://github.com/твой_username/reklamnyi-inspector-bot.git
```

### Ошибка: "failed to push some refs"

**Решение:**
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Ошибка: "authentication failed"

**Решение:**
- Используй Personal Access Token вместо пароля
- Или настрой SSH ключи (сложнее)

### Случайно загрузил .env

**Удали из Git:**
```bash
git rm --cached .env
git commit -m "Remove .env from repository"
git push
```

**Важно:** После этого создай новый токен бота! Старый мог попасть в историю Git.

---

## 🔄 Обновление кода на GitHub

После изменений в коде:

```bash
# Добавь изменения
git add .

# Создай коммит
git commit -m "Описание изменений"

# Загрузи на GitHub
git push
```

---

## 📝 Что дальше?

После загрузки на GitHub:

1. ✅ Подключи репозиторий к Render
2. ✅ Render автоматически задеплоит бота
3. ✅ При каждом `git push` бот автоматически обновится

**Инструкция:** См. [ДЕПЛОЙ_RENDER.md](ДЕПЛОЙ_RENDER.md)

---

## 💡 Советы

1. **Коммить часто** — после каждого значимого изменения
2. **Пиши понятные сообщения** — `git commit -m "Добавил проверку дисклеймера"`
3. **Не коммить .env** — всегда проверяй `git status` перед коммитом
4. **Используй ветки** — для экспериментов создавай отдельные ветки

---

**Готово!** Код на GitHub, можно подключать к Render! 🎉
