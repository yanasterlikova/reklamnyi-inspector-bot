# ✅ МИГРАЦИЯ НА RAILWAY ЗАВЕРШЕНА

## Что изменено

### 1. База данных (database.py)
- ✅ Добавлена поддержка PostgreSQL через `DATABASE_URL`
- ✅ Автоматическое определение: PostgreSQL (Railway) или SQLite (локально)
- ✅ Изменен синтаксис SQL запросов для PostgreSQL
- ✅ Обработка ошибок для обоих драйверов

### 2. Конфигурация (config.py)
- ✅ Добавлена поддержка `DATABASE_URL` из переменных окружения

### 3. Зависимости (requirements.txt)
- ✅ Добавлен `psycopg2-binary==2.9.9` для PostgreSQL

### 4. Файлы для Railway
- ✅ Создан `Procfile` — команда запуска бота
- ✅ Создан `railway.json` — конфигурация Railway

### 5. Документация
- ✅ Создан `ДЕПЛОЙ_RAILWAY.md` — пошаговая инструкция
- ✅ Обновлен `README.md` — добавлена информация о Railway
- ✅ Обновлен `СТАРТ_ЛИД_МАГНИТА.md` — добавлен Railway
- ✅ Обновлен `ПРОЕКТ_ГОТОВ.md` — информация о миграции

---

## Как использовать

### Локальная разработка (SQLite)

Если `DATABASE_URL` не установлен, бот автоматически использует SQLite:

```bash
# .env файл (без DATABASE_URL)
TELEGRAM_BOT_TOKEN=ваш_токен
ADMIN_CHAT_ID=ваш_chat_id

# Запуск
python bot.py
```

База данных создастся в `data/users.db`

---

### Railway (PostgreSQL)

Railway автоматически установит `DATABASE_URL`, бот определит PostgreSQL:

1. Залить код в GitHub
2. Создать проект в Railway
3. Подключить репозиторий
4. Добавить PostgreSQL (Railway автоматически установит `DATABASE_URL`)
5. Настроить переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `ADMIN_CHAT_ID`
6. Готово!

---

## Технические детали

### Изменения в SQL синтаксисе:

**SQLite → PostgreSQL:**
- `INTEGER PRIMARY KEY AUTOINCREMENT` → `SERIAL PRIMARY KEY`
- `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` → `TIMESTAMP DEFAULT NOW()`
- Плейсхолдеры `?` → `%s`
- `DATE('now')` → `CURRENT_DATE`
- `sqlite3.IntegrityError` → `psycopg2.IntegrityError`

### Автоматическое определение:

```python
DATABASE_URL = os.getenv("DATABASE_URL")

if DATABASE_URL:
    # PostgreSQL для Railway
    USE_POSTGRESQL = True
else:
    # SQLite для локальной разработки
    USE_POSTGRESQL = False
```

---

## Преимущества Railway

1. **Автоматический деплой** — push в GitHub → автоматическое обновление
2. **Автоперезапуск** — если бот упал, Railway перезапустит автоматически
3. **Логи в реальном времени** — смотреть логи в веб-интерфейсе
4. **Встроенный PostgreSQL** — не нужно настраивать отдельно
5. **SSL из коробки** — если понадобится webhook
6. **Метрики** — CPU, память, количество запросов

---

## Миграция данных (если нужно)

Если уже есть пользователи в SQLite на Beget:

1. Экспортируй данные из SQLite:
   ```bash
   sqlite3 data/users.db .dump > backup.sql
   ```

2. Импортируй в PostgreSQL на Railway (через Railway CLI или pgAdmin)

3. Или создай скрипт миграции (если нужно — могу помочь)

---

## Проверка работы

После деплоя на Railway:

1. Проверь логи в Railway (вкладка "Logs")
2. Должно быть: `INFO - 🔍 Рекламный Инспектор запущен!`
3. Открой бота в Telegram
4. Отправь `/start`
5. Пройди регистрацию
6. Отправь URL для проверки
7. Получи PDF-отчет

---

## Следующие шаги

1. ✅ Код готов к деплою на Railway
2. ⏭️ Залить код в GitHub
3. ⏭️ Создать проект в Railway
4. ⏭️ Задеплоить и протестировать

**Инструкция:** См. [ДЕПЛОЙ_RAILWAY.md](ДЕПЛОЙ_RAILWAY.md)

---

**Миграция завершена!** 🎉
