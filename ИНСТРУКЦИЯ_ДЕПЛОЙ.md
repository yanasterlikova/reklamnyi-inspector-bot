# 🚀 ИНСТРУКЦИЯ ПО ДЕПЛОЮ НА BEGET

## Шаг 1: Подключение к серверу

```bash
ssh root@2.56.240.113
# Пароль: vnJI0T57jxj%
```

---

## Шаг 2: Создание директории на сервере

```bash
mkdir -p /opt/reklamnyi_inspector
cd /opt/reklamnyi_inspector
```

---

## Шаг 3: Установка зависимостей на сервере

```bash
# Обновляем пакеты
apt update

# Устанавливаем Python и pip
apt install python3 python3-pip -y

# Устанавливаем системные зависимости для WeasyPrint
apt install -y \
    build-essential \
    python3-dev \
    python3-cffi \
    libcairo2 \
    libpango-1.0-0 \
    libpangocairo-1.0-0 \
    libgdk-pixbuf2.0-0 \
    libffi-dev \
    shared-mime-info

# Устанавливаем sshpass (для скриптов деплоя)
apt install sshpass -y
```

---

## Шаг 4: Копирование файлов с Mac

**На вашем Mac:**

```bash
cd "/Users/ya_neona/Library/Mobile Documents/com~apple~CloudDocs/Cursor/Бизнес/Команда Яны Рыжей/99_Системное/рекламный_инспектор_bot"

# Используем скрипт деплоя
bash DEPLOY_BEGET.sh
```

Или вручную:

```bash
sshpass -p 'vnJI0T57jxj%' rsync -avz \
    --exclude='.env' \
    --exclude='data/' \
    --exclude='__pycache__/' \
    ./ root@2.56.240.113:/opt/reklamnyi_inspector/
```

---

## Шаг 5: Настройка .env на сервере

**На сервере:**

```bash
cd /opt/reklamnyi_inspector
nano .env
```

Заполните:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_CHAT_ID=ваш_chat_id
LOG_LEVEL=INFO
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

---

## Шаг 6: Установка Python-зависимостей

```bash
cd /opt/reklamnyi_inspector
pip3 install -r requirements.txt
```

---

## Шаг 7: Создание systemd сервиса

```bash
# Копируем файл сервиса
cp reklamnyi-inspector.service /etc/systemd/system/

# Перезагружаем systemd
systemctl daemon-reload

# Включаем автозапуск
systemctl enable reklamnyi-inspector

# Запускаем бота
systemctl start reklamnyi-inspector

# Проверяем статус
systemctl status reklamnyi-inspector
```

---

## Шаг 8: Проверка работы

```bash
# Смотрим логи (в реальном времени)
journalctl -u reklamnyi-inspector -f

# Или последние 50 строк
journalctl -u reklamnyi-inspector -n 50
```

Должно быть:
```
INFO - 🔍 Рекламный Инспектор запущен!
```

---

## 📋 Управление ботом

### Команды systemd:

```bash
# Запустить
systemctl start reklamnyi-inspector

# Остановить
systemctl stop reklamnyi-inspector

# Перезапустить
systemctl restart reklamnyi-inspector

# Статус
systemctl status reklamnyi-inspector

# Логи
journalctl -u reklamnyi-inspector -f
```

### С Mac (через скрипты):

```bash
# Проверка статуса
bash ПРОВЕРКА_BEGET.sh

# Перезапуск
bash ПЕРЕЗАПУСК_BEGET.sh

# Деплой обновлений
bash DEPLOY_BEGET.sh
```

---

## 🔄 Обновление бота

**Когда нужно обновить код:**

1. Внесите изменения в локальные файлы (на Mac)
2. Запустите скрипт деплоя:
   ```bash
   bash DEPLOY_BEGET.sh
   ```
3. Бот автоматически перезапустится с новым кодом

---

## 🐛 Troubleshooting

### Бот не запускается

```bash
# Смотрим логи
journalctl -u reklamnyi-inspector -n 100

# Проверяем .env
cat /opt/reklamnyi_inspector/.env

# Проверяем зависимости
cd /opt/reklamnyi_inspector
pip3 list | grep telegram
```

### Ошибки WeasyPrint

```bash
# Переустановите системные библиотеки
apt install --reinstall libcairo2 libpango-1.0-0
```

### Проблемы с правами

```bash
# Дайте права на папку
chmod -R 755 /opt/reklamnyi_inspector
chown -R root:root /opt/reklamnyi_inspector
```

---

## ✅ ЧЕКЛИСТ ДЕПЛОЯ

После деплоя проверь:

- [ ] Бот запущен: `systemctl status reklamnyi-inspector`
- [ ] Логи без ошибок: `journalctl -u reklamnyi-inspector -n 20`
- [ ] Бот отвечает в Telegram на `/start`
- [ ] Регистрация работает
- [ ] Проверка URL работает
- [ ] PDF генерируется
- [ ] Уведомления админу приходят

---

**Важно:** НЕ запускайте бота локально на Mac, если он запущен на сервере! Только один экземпляр бота может работать одновременно.
